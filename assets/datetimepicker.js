$(function () {
    $('.dtpicker').datetimepicker({
        locale: 'cs',
        format: 'YYYY-MM-DD HH:mm',
    });

    $('.dpicker').datetimepicker({
        locale: 'cs',
        format: 'YYYY-MM-DD',
    });

    $('.tpicker').datetimepicker({
        locale: 'cs',
        format: 'HH:mm',
    });
});
